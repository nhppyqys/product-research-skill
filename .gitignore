node_modules/
dist/
.DS_Store
.env
.env.*

# Evidence manifests are working files and never ship with a report.
# The example under docs/ is deliberately kept.
*.evidence.json
!docs/examples/*.evidence.json
